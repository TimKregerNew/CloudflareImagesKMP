# Kotlin
kotlin.code.style=official
kotlin.mpp.enableCInteropCommonization=true
kotlin.mpp.androidSourceSetLayoutVersion=2

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# Gradle
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Library Version
library.version=1.0.0

# JFrog Artifactory Configuration
# You can also set these as environment variables:
# export ARTIFACTORY_URL=https://your-company.jfrog.io/artifactory/libs-release-local
# export ARTIFACTORY_USERNAME=your-username
# export ARTIFACTORY_PASSWORD=your-password
#
# Or set them here (NOT recommended for credentials - use env vars or local.properties):
# artifactory.url=https://your-company.jfrog.io/artifactory/libs-release-local
# artifactory.username=
# artifactory.password=
